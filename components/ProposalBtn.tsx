"use client";
import React from "react";
import { Button } from "./ui/button";

type Props = {};

const ProposalBtn = (props: Props) => {
  return (
    <Button variant={"outline"} onClick={() => {}} className="  mt-3 ">
      Get Started
    </Button>
  );
};
export default ProposalBtn;
